import 'package:flutter/material.dart';

class TezaursView extends StatelessWidget {
  const TezaursView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tezaurs Page Content'),
    );
  }
}